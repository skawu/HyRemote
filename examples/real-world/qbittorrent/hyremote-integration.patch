diff --git a/src/app/CMakeLists.txt b/src/app/CMakeLists.txt
index acb8378..8d80592 100644
--- a/src/app/CMakeLists.txt
+++ b/src/app/CMakeLists.txt
@@ -133,6 +133,10 @@ endif()
 # -----------------------------------------------------------------------------
 if (GUI)
     target_link_libraries(qbt_app PRIVATE qbt_gui)
+# --- HyRemote integration study (see examples/real-world/qbittorrent) ---
+find_package(HyRemote CONFIG REQUIRED)
+target_link_libraries(qbt_app PRIVATE HyRemote::RemoteAccess)
+hyremote_deploy(TARGET qbt_app)
     if ((CMAKE_SYSTEM_NAME STREQUAL "Windows") OR (CMAKE_SYSTEM_NAME STREQUAL "Darwin"))
         qt_import_plugins(qbt_app INCLUDE Qt::QSvgIconPlugin Qt::QSvgPlugin)
     endif()
