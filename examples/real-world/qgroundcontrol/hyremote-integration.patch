diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 1e3e7b1..9e63e0f 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -150,6 +150,10 @@ target_link_libraries(${CMAKE_PROJECT_NAME}
 # Optional/Conditional Modules
 # ----------------------------------------------------------------------------
 target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE AutoPilotPluginsAPMModule)
+# --- HyRemote integration study (see examples/real-world/qgroundcontrol) ---
+find_package(HyRemote CONFIG REQUIRED)
+target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE HyRemote::RemoteAccess)
+hyremote_deploy(TARGET ${CMAKE_PROJECT_NAME} QML)
 target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE AutoPilotPluginsPX4Module)
 
 target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE Viewer3DModule)
