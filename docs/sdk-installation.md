# 已迁移 / Moved

本文档的内容已合并进 **[安装、构建与集成准备](guide/install.md)**（中文）｜
**[Install, build and integration preparation](en/guide/install.md)**（English）。

本文件只是一个迁移指针：它之所以还存在，是因为发布就绪门禁当前仍列出此路径。它**不承载文档内容**；
请阅读上面的规范文档。移除该指针需要先更新门禁路径清单（属维护者变更）。

This file is a migration pointer retained only because the release-readiness metadata gate still lists this
path. It carries no documentation content - read the canonical document above. Removing it requires updating
the gate's path list, which is a maintainer change.
